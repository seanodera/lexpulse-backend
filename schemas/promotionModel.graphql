
type Promotion {
    id: ID!
    eventId: ID!
    eventHostId: ID!
    promotionLevel: String!
    startDate: String!
    endDate: String!
    country: String!
}

input PromotionInput {
    eventId: ID!
    eventHostId: ID!
    promotionLevel: String!
    startDate: String
    endDate: String!
    country: String!
}

